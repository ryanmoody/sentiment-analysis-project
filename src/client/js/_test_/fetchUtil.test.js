import { fetchSentimentAnalysis } from '../fetchUtil';

describe('fetchUtil tests', () => {
  test('fetchSentimentAnalysis should be defined', () => {
    expect(fetchSentimentAnalysis).toBeDefined();
  });
});
